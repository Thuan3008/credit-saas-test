credit-saas-test/
│
├── docker-compose.yml
│   └── Cấu hình chạy toàn bộ project bằng Docker: PostgreSQL + backend + frontend sau này
│
├── .gitignore
│   └── Khai báo các file/folder không đưa lên GitHub như __pycache__, .env, node_modules
│
├── backend/
│   │
│   ├── Dockerfile
│   │   └── Cấu hình build image Docker cho backend FastAPI
│   │
│   ├── requirements.txt
│   │   └── Danh sách thư viện Python cần cài cho backend
│   │
│   └── app/
│       │
│       ├── __init__.py
│       │   └── Đánh dấu thư mục app là một Python package
│       │
│       ├── main.py
│       │   └── File chạy chính của FastAPI, khởi tạo app, CORS, tạo bảng, gắn router API
│       │
│       ├── database.py
│       │   └── Cấu hình kết nối PostgreSQL, tạo engine, session, Base cho SQLAlchemy
│       │
│       ├── models/
│       │   └── Chứa các class đại diện cho bảng trong database
│       │
│       │   ├── __init__.py
│       │   │   └── Đánh dấu thư mục models là Python package
│       │   │
│       │   └── user.py
│       │       └── Định nghĩa bảng users: email, password_hash, full_name, role, created_at
│       │
│       ├── schemas/
│       │   └── Chứa các Pydantic schema dùng để validate request và format response
│       │
│       │   ├── __init__.py
│       │   │   └── Đánh dấu thư mục schemas là Python package
│       │   │
│       │   └── auth_schema.py
│       │       └── Định nghĩa dữ liệu cho register, login, user response, token response
│       │
│       ├── routers/
│       │   └── Chứa các file định nghĩa API endpoint
│       │
│       │   ├── __init__.py
│       │   │   └── Đánh dấu thư mục routers là Python package
│       │   │
│       │   ├── auth.py
│       │   │   └── API đăng ký, đăng nhập, lấy thông tin user hiện tại
│       │   │
│       │   ├── seed.py
│       │   │   └── API tạo dữ liệu mẫu, hiện tại dùng để tạo tài khoản admin
│       │   │
│       │   └── admin_test.py
│       │       └── API test quyền admin, dùng để kiểm tra require_admin hoạt động đúng chưa
│       │
│       ├── dependencies/
│       │   └── Chứa các dependency/middleware dùng chung cho API
│       │
│       │   ├── __init__.py
│       │   │   └── Đánh dấu thư mục dependencies là Python package
│       │   │
│       │   └── auth_dependency.py
│       │       └── Kiểm tra JWT token, lấy current_user, kiểm tra quyền admin
│       │
│       └── services/
│           └── Chứa logic xử lý nghiệp vụ dùng lại nhiều nơi
│
│           ├── __init__.py
│           │   └── Đánh dấu thư mục services là Python package
│           │
│           └── auth_service.py
│               └── Hash password, verify password, tạo JWT access token
│
└── frontend/
    └── Tạm thời chưa làm, Phase sau sẽ tạo ReactJS ở đây